# --- Python ---
__pycache__/
**/__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# --- Virtual environments ---
.venv/
venv/
env/

# --- Secrets / local config (keep .env.example tracked) ---
.env

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp

# --- Heavy raw source data: CSVs under resources/ (kept out of the repo) ---
resources/**/*.csv
# ...except the tiny web-scraping demo fixture, so `--add SCRAPER` and the notebook work out of the box.
!resources/SCRAPED/**/*.sample.csv

# --- Embedding cache (regenerated; large binary) ---
kb/.emb_cache_*.pkl

# --- Docs (LaTeX report + build artifacts) ---
docs/

# --- Personal command cheat-sheet (not part of the repo) ---
COMMANDS.md