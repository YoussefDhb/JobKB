# Core pipeline
numpy
pandas
scikit-learn
sentence-transformers        # bge-m3 embedder (candidate generation / semantic checks)
transformers                 # mDeBERTa NLI verifier + local LLM fallback + NLLB MT
torch
huggingface_hub              # HF Inference Providers client (LLM enrichment)
sentencepiece                # tokenizer for facebook/nllb-200 (bilingual label MT)

# Agentic enrichment + graph export
langgraph                    # LangGraph controller + reflective workers (--agent)
rdflib                       # RDF/OWL Turtle export (--export rdf)

# Web-scraping enrichment (--scrape); the crawler itself needs only the stdlib
beautifulsoup4               # resilient HTML parsing for the job-board adapters

# Notebook (verification & testing)
notebook
ipywidgets
matplotlib
networkx                     # graph analytics use case (centrality / structural bridges) in inspect.ipynb
